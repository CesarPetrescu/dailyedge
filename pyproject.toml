[project]
name = "dailyedge"
version = "0.1.0"
description = "Alert-only trading research stack for DailyEdge v2."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = [".", "scripts"]
testpaths = ["tests"]
